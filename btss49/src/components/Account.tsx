import React from 'react';

const Account: React.FC = () => {
  return (
    <div className="flex items-center justify-center h-screen bg-gray-100">
      <h1 className="text-3xl font-bold">Đây là trang Account</h1>
    </div>
  );
};

export default Account;
